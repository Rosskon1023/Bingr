<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>Movie Edit</title>
</head>
<body>
        <header>
            <h1>Bingr | Edit</h1>
            <a href="/movies/">Return to Movie Home</a>
        </header>
        <img src=<%= movie.img %>>
        <form action="/movies/<%=movie._id%>?_method=PUT" method="POST">
            Have You Watched? <input type="checkbox" name="watched" <%if(movie.watched === true){ %> checked <% } %> /><br />
            <label for="rating">Rating (Between 0 and 10):</label>
                <input type="number" id="rating" name="user_rating" min="0" max="10" step="0.1" value="<%=movie.user_rating%>">
            <input type="submit" value="Edit Movie"/>
        </form>
</html>