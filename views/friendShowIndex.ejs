<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Courier+Prime&family=Fugaz+One&family=Jura:wght@700&family=Nosifer&family=Nunito:wght@300&family=Ubuntu:wght@500&display=swap" rel="stylesheet">
    <link rel="icon" href="/public/favicon.ico">
    <title>Bingr | All Shows</title>
  </head>
  <body>
    <h1>Bingr - Friend Show Library</h1>
    <header>
        <nav class="navBar">
            <a class="navItem" href="/dashboard">Home</a>
            <a class="navItem" href="/movies">My Movies</a>
            <a class="navItem" href="/shows">My Shows</a>
            <a class="navItem" href="/friends">Friends</a>
            <a class="navItem" href="/logout">Logout</a>
        </nav>
    </header>
    <div class="libraryContainer">
        <div class="sublibraryContainer1">
            <h3 class="sublibraryTitle">&#x1F39E; Bing'd It! &#x1F39E;</h3>
            <div class="library-item-container">
                        <% shows.forEach(show => { %>
                        <% if (show.watched === true) { %>
                        <div class="friend-index-flex-item">
                            <p class="flex-item-title"><%=show.title%></p>
                            <p class="flex-item-title">Rating: <%=show.user_rating%></p>
                            <a class="flex-item-image" href="/friends/shows/<%=show._id%>">
                                <img src=<%=show.img%>>
                            </a>
                            <form class="friendNewProgram" action="/friends/<%=show.user_id%>/shows" method="POST">
                                <input type="hidden" name="title" value="<%=show.title%>">
                                <input id="new_button" type="submit" value="Add Show"/>
                            </form>
                        </div>
                        <% } %>
                        <% }) %>
            </div>         
        </div>
        <div class="sublibraryContainer2">
            <h3 class="sublibraryTitle">&#x1F3AC; Up Next! &#x1F3AC;</h3>
            <div class="library-item-container">
                        <% shows.forEach(show => { %>
                        <% if (show.watched === false) { %>
                        <div class="friend-index-flex-item">
                            <p class="flex-item-title"><%=show.title%></p>
                            <p class="flex-item-title">Rating: TBD</p>
                            <a class="flex-item-image" href="/friends/shows/<%=show._id%>">
                                <img src=<%=show.img%>>
                            </a>
                            <form class="friendNewProgram" action="/friends/<%=show.user_id%>/shows" method="POST">
                                <input type="hidden" name="title" value="<%=show.title%>">
                                <input id="new_button" type="submit" value="Add Show"/>
                            </form>
                        </div>
                        <% } %>
                        <% }) %>
            </div>            
        </div>
    </div>
    <%- include("./partials/footer.ejs")%>
  </body>
</html>